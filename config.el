;; Define the buffer local company backend variable
;; (spacemacs|defvar-company-backends ipl-mode)

(defvar ipl-path-to-language-server nil
  "Path to the IPL language server executable.")

(defvar ipl-java-home nil
  "Path to JAVA_HOME directory.")
